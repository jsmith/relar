// This is shared so I created a file to put it in
export const LOCAL_CACHE_TEXT = `Your files are all stored locally to improve performance. As there may still be kinks with
the algorithm that keeps things in sync, you can use this to synchronize your local files
with the cloud.`;
