diff --git a/node_modules/fast-average-color/dist/index.esm.js b/node_modules/fast-average-color/dist/index.esm.js
index b7ca0e2..4786509 100644
--- a/node_modules/fast-average-color/dist/index.esm.js
+++ b/node_modules/fast-average-color/dist/index.esm.js
@@ -13,8 +13,7 @@ function dominantAlgorithm(arr, len, options) {
         ignoredColor = options.ignoredColor;
 
     for (let i = 0; i < len; i += options.step) {
-        let
-            red = arr[i],
+        let red = arr[i],
             green = arr[i + 1],
             blue = arr[i + 2],
             alpha = arr[i + 3];
@@ -68,8 +67,7 @@ function dominantAlgorithm(arr, len, options) {
 }
 
 function simpleAlgorithm(arr, len, options) {
-    let
-        redTotal = 0,
+    let redTotal = 0,
         greenTotal = 0,
         blueTotal = 0,
         alphaTotal = 0,
@@ -104,8 +102,7 @@ function simpleAlgorithm(arr, len, options) {
 }
 
 function sqrtAlgorithm(arr, len, options) {
-    let
-        redTotal = 0,
+    let redTotal = 0,
         greenTotal = 0,
         blueTotal = 0,
         alphaTotal = 0,
@@ -316,8 +313,7 @@ class FastAverageColor {
     }
 
     _prepareSizeAndPosition(originalSize, options) {
-        let
-            srcLeft = this._getOption(options, 'left', 0),
+        let srcLeft = this._getOption(options, 'left', 0),
             srcTop = this._getOption(options, 'top', 0),
             srcWidth = this._getOption(options, 'width', originalSize.width),
             srcHeight = this._getOption(options, 'height', originalSize.height),
